/**
 * 
 */

function myFunction(letter)
{
	  var currCell_element = document.getElementById(`cell-0-0`);
	  var fill = `<p><font color="white"> ${letter} </font></p>`
	  currCell_element.innerHTML = fill
}
